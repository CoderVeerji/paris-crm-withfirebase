{
  "name": "paris-crm-migration",
  "version": "1.0.0",
  "private": true,
  "description": "One-time migration: Google Sheets (copy) -> Firestore",
  "scripts": {
    "validate": "node validate.js",
    "migrate": "node migrate.js",
    "migrate:wipe": "node migrate.js --wipe",
    "migrate:dry": "node migrate.js --dry",
    "migrate:test": "node migrate.js --wipe --limit 2500 --sample"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "firebase-admin": "^12.7.0"
  }
}
